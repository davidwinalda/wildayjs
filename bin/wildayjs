#!/usr/bin/env node
const { showHelp, errors } = require("../lib/utils/chalkUtils");
const ModelGenerator = require("../lib/models/modelGenerator");
const ControllerGenerator = require("../lib/controllers/controllerGenerator");
const listDatabases = require("../lib/database/lists");
const {
  initDatabase,
  switchDatabase,
  DATABASE_TYPES,
} = require("../lib/database/init");
const {
  generateMigration,
  applyMigrations,
  startConsole,
  dbStatus,
  checkDatabase,
  newApp,
} = require("../index");
const { program } = require("commander");
const path = require("path");

// Read version from package.json
const packageJson = require(path.join(__dirname, "..", "package.json"));

program
  .version(packageJson.version)
  .description("WildayJS - A Full-Stack JavaScript Web Framework");

// New app command
program
  .command("new <app-name>")
  .description("Create a new WildayJS application")
  .action((appName) => {
    if (!appName) {
      errors.missingArgument("app name");
      process.exit(1);
    }
    newApp(appName);
  });

// Database commands
program
  .command("db:init")
  .description("Initialize multi-database configuration")
  .option(
    "-t, --type <type>",
    `Set active database type (${DATABASE_TYPES.join("|")})`,
    "sqlite"
  )
  .option("-f, --force", "Force overwrite existing configuration", false)
  .action((options) => {
    const { type, force } = options;

    if (!DATABASE_TYPES.includes(type)) {
      console.error(`Error: Invalid database type '${type}'`);
      console.log(`Supported types: ${DATABASE_TYPES.join(", ")}`);
      process.exit(1);
    }

    console.log("\nInitializing multi-database configuration with options:", {
      activeType: type,
      force,
    });

    initDatabase({ type, force }).catch((error) => {
      console.error("Database initialization failed:", error.message);
      process.exit(1);
    });
  });

program
  .command("db:switch")
  .description("Switch active database")
  .argument(
    "<type>",
    `Database type to switch to (${DATABASE_TYPES.join("|")})`
  )
  .action((newType) => {
    if (!DATABASE_TYPES.includes(newType)) {
      console.error(`Error: Invalid database type '${newType}'`);
      console.log(`Supported types: ${DATABASE_TYPES.join(", ")}`);
      process.exit(1);
    }

    console.log(`\nSwitching active database to: ${newType}`);

    switchDatabase(newType)
      .then(() => {
        console.log(`Successfully switched to ${newType} database`);
      })
      .catch((error) => {
        console.error("Failed to switch database:", error.message);
        process.exit(1);
      });
  });

program
  .command("db:status")
  .description("Show database migration status and configuration")
  .option(
    "-t, --type <type>",
    `Check specific database type (${DATABASE_TYPES.join("|")})`,
    null
  )
  .action((options) => {
    if (options.type && !DATABASE_TYPES.includes(options.type)) {
      console.error(`Error: Invalid database type '${options.type}'`);
      console.log(`Supported types: ${DATABASE_TYPES.join(", ")}`);
      process.exit(1);
    }
    dbStatus(options.type);
  });

program
  .command("db:check")
  .description("Check database connection and structure")
  .option(
    "-t, --type <type>",
    `Check specific database type (${DATABASE_TYPES.join("|")})`,
    null
  )
  .action((options) => {
    if (options.type && !DATABASE_TYPES.includes(options.type)) {
      console.error(`Error: Invalid database type '${options.type}'`);
      console.log(`Supported types: ${DATABASE_TYPES.join(", ")}`);
      process.exit(1);
    }
    checkDatabase(options.type);
  });

program
  .command("db:list")
  .description("List all configured databases and their status")
  .action(listDatabases);

// Generate commands
program
  .command("generate:controller <name> [actions...]")
  .description("Generate a new controller")
  .option("--api", "Generate an API controller")
  .option("--skip-views", "Skip generating views")
  .option("--skip-routes", "Skip adding routes")
  .action((name, actions, options) => {
    if (!name) {
      console.error("Controller name is required");
      process.exit(1);
    }
    const generator = new ControllerGenerator(name, actions, options);
    generator.generate();
  });

program
  .command("generate:model <name> [attributes...]")
  .description("Generate a new model")
  .action((name, attributes) => {
    if (!name) {
      console.error("Model name is required");
      process.exit(1);
    }
    const generator = new ModelGenerator(name, attributes);
    generator.generate();
  });

program
  .command("generate:migration <name> [args...]")
  .description("Generate a new migration")
  .option("-c, --columns <columns>", "Specify columns for the migration")
  .action((name, args, options) => {
    if (!name) {
      errors.migrationNameRequired();
      process.exit(1);
    }

    // Support both inline args and --columns option
    let columns = args;
    if (options.columns) {
      columns = options.columns.split(/\s+/);
    }

    // For direct format, combine name and args into a single migration string
    const migrationName =
      columns.length > 0 ? `${name} ${columns.join(" ")}` : name;

    // Support both legacy and direct formats
    const validLegacyPattern =
      /^(Create|Add\w+To|Change\w+In|Remove\w+From|Rename\w+To\w+In)\w+/;
    const validDirectPattern = /^(\w+):(\w+)\s+(\w+)/;

    if (
      !validLegacyPattern.test(name) &&
      !validDirectPattern.test(migrationName)
    ) {
      errors.invalidMigrationFormat();
      process.exit(1);
    }

    if (name.startsWith("Create") && (!columns || columns.length === 0)) {
      errors.columnsRequired();
      process.exit(1);
    }

    generateMigration(migrationName, columns);
  });

// Migration commands
program
  .command("db:migrate")
  .description("Run pending migrations")
  .action(() => applyMigrations());

program
  .command("db:rollback")
  .description("Rollback migrations")
  .option("--step <number>", "Number of migrations to rollback", "1")
  .action((options) => {
    const steps = parseInt(options.step, 10);
    if (isNaN(steps) || steps < 1) {
      errors.invalidStepNumber();
      process.exit(1);
    }
    applyMigrations({ command: "rollback", steps });
  });

program
  .command("db:migrate:up [version]")
  .description("Run a specific migration up")
  .action((version) => {
    if (!version || !/^\d{14}$/.test(version)) {
      errors.invalidVersion();
      process.exit(1);
    }
    applyMigrations({ command: "migrate:up", version });
  });

program
  .command("db:migrate:down [version]")
  .description("Run a specific migration down")
  .action((version) => {
    if (!version || !/^\d{14}$/.test(version)) {
      errors.invalidVersion();
      process.exit(1);
    }
    applyMigrations({ command: "migrate:down", version });
  });

program
  .command("db:migrate:reset")
  .description("Reset database by running all migrations down and up")
  .action(() => applyMigrations({ reset: true }));

program
  .command("console")
  .description("Start WildayJS console")
  .action(() => startConsole());

// Help command
program
  .command("help")
  .description("Display help information")
  .action(() => showHelp());

program.parse(process.argv);

// Show help if no arguments provided
if (!process.argv.slice(2).length) {
  program.outputHelp();
}
